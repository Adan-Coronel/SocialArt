doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title SocialArt | Red social para artesanos
    link(rel="stylesheet", href="/login.css") 
    link(rel="stylesheet", href="/perfil.css")
  body
    header
      nav.navbar
        ul.nav-left
          li: a(href="/muro") Inicio
          li: a(href="/perfil") Mi perfil
          li: a(href="/albums") Mis álbumes
        ul.nav-right
          if typeof usuarioLogueado !== 'undefined'
            li
              form(action="/busqueda", method="GET", style="display:inline;")
                input(type="text", name="q", placeholder="Buscar por nombre/email")
                button(type="submit") Buscar
          li: a(href="/logout") Cerrar sesión

    main
      block content

    footer
      p © 2025 SocialArt. Adan Coronel .